# Mappings

The executable mapping contract currently lives in code:

- `migration_toolkit.audit.ENTITY_MAPPINGS`
- `migration_toolkit.importer.PHASE_ORDER`
- `migration_toolkit.importer.PHASE_TARGET_TABLES`
- `migration_toolkit.importer.SOURCE_COUNT_SQL`

Keep this directory for future YAML exports if the mapping needs to become editable without changing Python code.
