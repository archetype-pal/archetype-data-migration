= public.publications_event (new-only) — legacy source investigation

== Summary
* **Rows (new):** 0
* **Status:** Table exists in the new schema but is currently **unused/empty**.
* **Old pair:** No obvious 1:1 legacy table exists (no old tables matching `*event*` by name).
* **Possible legacy origin:** A small subset of legacy blog posts appear “event-like” by keyword search (**5** posts contain “event” in title/keywords/description), but no migration into `publications_event` is evidenced in the current data.

== Counts
|===
|DB|Table|Rows
|new|public.publications_event|0
|===

== Column model (new)
|===
|Ordinal|Column|Type|Nullable|Notes
|1|`id`|bigint|NO|PK
|2|`title`|varchar(150)|NO|
|3|`slug`|varchar(150)|NO|Unique
|4|`content`|text|NO|
|5|`created_at`|timestamptz|NO|
|6|`updated_at`|timestamptz|NO|
|===

== Constraints and indexes

=== Constraints (new)
* Primary key on `id`
* Unique constraint on `slug`

=== Indexes (new)
* PK index on `id`
* Unique index on `slug`
* Pattern-ops index on `slug` (`varchar_pattern_ops`)

== Data quality / blocker checks
Because the table is **empty**, data-quality checks (NULLs, duplicate slugs) are trivially satisfied.

== Old-candidate discovery (legacy)
Findings from old-schema scanning:

* **No old tables named like “event”** were found (`table_name ILIKE '%event%'` returned empty).
* By column-shape heuristics, many legacy tables have typical CMS fields, with `blog_blogpost` scoring highest on “title/slug/content/time” style columns.
* A lightweight semantic scan shows **5 legacy blog posts** matching the keyword “event” in `title`, `keywords_string`, or `description`.

== Interpretation
The evidence supports that:

* `publications_event` is a **new feature table** intended for future use (events section), not currently populated.
* Any event-like content in the legacy system likely remained as part of the blog/publication content (`blog_blogpost` → `publications_publication`), rather than being split into a dedicated events model.
