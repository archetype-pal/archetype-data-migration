= public.digipal_description (old) → public.manuscripts_historicalitemdescription (new)

== Summary
* Rows: 703 → 703 (complete transfer; IDs preserved)
* Schema simplification and semantic rename:
** old multiple text fields (`description`, `comments`, `summary`) replaced by a single new `content` field
** old `text_id` removed from the model
* `historical_item_id` hardened from nullable to NOT NULL (safe: old NULL count = 0)
* `source_id` preserved and retargeted to the new bibliographic source table
* `content` is an exact carry-over of old `description` for all rows

== Counts
|===
|DB|Table|Rows
|old|public.digipal_description|703
|new|public.manuscripts_historicalitemdescription|703
|===

== Direct row identity preservation
|===
|Check|Result
|Missing IDs after migration|0
|Conclusion|All description entities were preserved by ID
|===

== Column changes
|===
|Column|Old|New|Notes
|id|int4 NOT NULL|int8 NOT NULL|type widened
|historical_item_id|int4 NULLABLE|int8 NOT NULL|safe hardening; old NULLs = 0
|source_id|int4 NOT NULL|int8 NOT NULL|type widened; FK retargeted to `manuscripts_bibliographicsource`
|description|text NULLABLE|—|migrated to new `content`
|comments|text NULLABLE|—|dropped (effectively empty/blank in migrated rows)
|summary|varchar(256) NULLABLE|—|dropped (effectively empty/blank in migrated rows)
|text_id|int4 NULLABLE|—|dropped (and old uniqueness changed)
|created|timestamptz NOT NULL|—|dropped
|modified|timestamptz NOT NULL|—|dropped
|content|—|text NOT NULL|new canonical description text field
|===

== Constraints and indexes
Old:

* FK `historical_item_id` → `digipal_historicalitem(id)`
* FK `source_id` → `digipal_source(id)`
* FK `text_id` → `digipal_text(id)`
* UNIQUE (`source_id`, `text_id`, `historical_item_id`)

New:

* FK `historical_item_id` → `manuscripts_historicalitem(id)`
* FK `source_id` → `manuscripts_bibliographicsource(id)`
* Primary key on `id`
* No equivalent DB-level unique constraint including `text_id` (column removed)

== Data quality / blocker checks (generic same-column checks)
|===
|Check|Result
|Old NULLs in common new NOT NULL columns (`id`, `historical_item_id`, `source_id`)|0 for all
|Old values exceeding new varchar lengths (common string columns)|None detected / not applicable
|===

== Semantic migration verification

=== Relationship field preservation
Verified by joining old/new rows on `id`.

|===
|Check|Result
|Total rows compared|703
|`historical_item_id` exact matches|703
|`historical_item_id` changed|0
|`source_id` exact matches|703
|`source_id` changed|0
|===

Conclusion:

* `historical_item_id` and `source_id` were preserved exactly by row ID.

=== `content` derivation
Verified by comparing new `content` against old `description`, `summary`, and `comments` by `id`.

|===
|Check|Result
|Total rows compared|703
|New `content` NULLs|0
|New `content` blank-or-null|0
|Rows where `new.content = old.description`|703
|Rows where `new.content = old.summary`|0
|Rows where `new.content = old.comments`|0
|Mismatches against expected mapping|0
|===

Old field population profile (same 703 rows):
|===
|Field|Observation
|`description`|0 NULLs; 0 blank-or-null (fully populated)
|`summary`|576 NULLs; 703 blank-or-null (effectively empty)
|`comments`|576 NULLs; 703 blank-or-null (effectively empty)
|===

Conclusion:

* `new.content` is an exact semantic carry-over of `old.description`.
* `summary` and `comments` did not contribute content for migrated rows in this table (effectively blank/empty).

== Conclusion
This table migrated completely and cleanly:

* row count and IDs preserved
* relationship fields preserved exactly (`historical_item_id`, `source_id`)
* descriptive text consolidated into a single required field (`content`)
* old auxiliary text fields (`summary`, `comments`) and `text_id` removed as part of schema simplification
