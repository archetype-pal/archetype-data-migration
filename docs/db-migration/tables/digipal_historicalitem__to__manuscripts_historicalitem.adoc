= public.digipal_historicalitem (old) → public.manuscripts_historicalitem (new)

== Summary
* Rows: 713 → 713 (complete transfer; IDs preserved)
* Major schema remodel (only common column is `id`)
* Semantic mappings verified for `type`, `language`, and `format_id`
* `hair_type` is NULL for all rows because old `hair_id` was NULL for all rows
* Date values were normalized into `common_date` via `date_id` and are not direct text carry-overs from old `date` / `date_sort`

== Counts
|===
|DB|Table|Rows
|old|public.digipal_historicalitem|713
|new|public.manuscripts_historicalitem|713
|===

== Direct row identity preservation
|===
|Check|Result
|Missing IDs after migration|0
|Conclusion|All historical item entities were preserved by ID
|===

== Column model comparison

=== Old-only columns (removed/relocated/re-modeled)
* `catalogue_number`
* `created`
* `date`
* `date_sort`
* `display_label`
* `hair_id`
* `historical_item_format_id`
* `historical_item_type_id`
* `language_id`
* `legacy_id`
* `legacy_reference`
* `modified`
* `name`
* `neumed`
* `url`
* `vernacular`

=== New-only columns (new representation)
* `date_id` (FK to `common_date`)
* `format_id` (FK to `manuscripts_itemformat`)
* `hair_type` (varchar)
* `language` (varchar)
* `type` (varchar, NOT NULL)

=== Common columns
* `id` only (`int4` → `int8`)

== Constraints and indexes
Old:

* FKs to old lookup tables:
** `hair_id` → `digipal_hair(id)`
** `historical_item_format_id` → `digipal_format(id)`
** `historical_item_type_id` → `digipal_historicalitemtype(id)`
** `language_id` → `digipal_language(id)`

New:

* FKs to new lookup tables:
** `date_id` → `common_date(id)`
** `format_id` → `manuscripts_itemformat(id)`

== Data quality / blocker checks (generic same-column checks)
|===
|Check|Result
|Old NULLs in common new NOT NULL columns|Only `id` checked; 0 NULLs
|Old values exceeding new varchar lengths (common string columns)|Not applicable (no common string columns)
|===

== Semantic migration verification

=== `type` mapping (lookup-based)
Verified by joining old `historical_item_type_id` to `digipal_historicalitemtype.name` and comparing to `new.type` by `id`.

|===
|Check|Result
|Total rows compared|713
|New `type` NULLs|0
|Old type name NULLs|0
|Exact matches (`new.type = old type name`)|713
|Differences|0
|===

Conclusion:

* `new.type` is an exact semantic carry-over of the old historical item type lookup name.

=== `language` mapping (lookup-based)
Verified by joining old `language_id` to `digipal_language.name` and comparing to `new.language` by `id`.

|===
|Check|Result
|Total rows compared|713
|Old `language_id` NULLs|11
|New `language` NULLs|11
|Exact matches (`new.language = old language name`)|702
|Differences|0
|===

Conclusion:

* `new.language` is an exact semantic carry-over of the old language lookup name where present.
* NULL language values were preserved (11 rows).

=== `format_id` mapping
Verified by comparing old `historical_item_format_id` to `new.format_id` by `id`.

|===
|Check|Result
|Total rows compared|713
|Old `historical_item_format_id` NULLs|633
|New `format_id` NULLs|633
|Exact matches (`new.format_id = old historical_item_format_id`)|80
|Differences|0
|===

Conclusion:

* `new.format_id` is a direct carry-over of old `historical_item_format_id`, including NULL preservation.

=== `hair_type` mapping status
Verified by comparing old `hair_id` (and lookup label) to `new.hair_type`.

|===
|Check|Result
|Total rows compared|713
|Old `hair_id` NULLs|713
|Old `hair_id` non-NULLs|0
|New `hair_type` NULLs|713
|New `hair_type` non-NULLs|0
|===

Conclusion:

* No hair values existed in old data for this table (`hair_id` always NULL), so `new.hair_type` remaining NULL is consistent and not a migration loss.

=== Date normalization (`date_id` → `common_date`)
Verified by comparing `new.date_id -> common_date.date` text against old `date` and old `date_sort` by `id`.

|===
|Check|Result
|Total rows compared|713
|New `date_id` NULLs|0
|`common_date.date` NULLs|0
|`common_date.date = old.date`|0
|`common_date.date = old.date_sort`|0
|`common_date.date` differs from both old fields|713
|===

Conclusion:

* Date information in the new schema is fully populated and normalized via `common_date`, but it is not a direct textual carry-over from `digipal_historicalitem.date` or `date_sort`.
* This indicates a distinct date transformation/normalization process was applied during migration.

== Conclusion
This table migrated completely at the entity level (all IDs preserved) and demonstrates a strong semantic migration:

* lookup IDs converted to meaningful text fields (`type`, `language`)
* format FK preserved directly (`historical_item_format_id` → `format_id`)
* date data normalized into `common_date` via `date_id`
* absent legacy hair values consistently remain absent (`hair_type` NULL for all rows)
