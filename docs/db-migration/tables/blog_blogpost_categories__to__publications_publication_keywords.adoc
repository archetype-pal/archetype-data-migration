= public.blog_blogpost_categories (old) → public.publications_publication_keywords (new)

== Summary
* Rows: 67 → 67 (bridge table preserved)
* Pair-set semantics preserved exactly: distinct post↔category links are identical (0 missing / 0 extra)
* FK integrity clean on new side (0 publication or tag orphans)
* This bridge links:
** old: `blogpost_id` → `blog_blogpost.id`, `blogcategory_id` → `blog_blogcategory.id`
** new: `publication_id` → `publications_publication.id`, `tagulous_publication_keywords_id` → `publications_tagulous_publication_keywords.id`
* Because:
** `blog_blogpost.id` is preserved as `publications_publication.id` (61/61)
** `blog_blogcategory.id` is preserved as `publications_tagulous_publication_keywords.id` (3/3)
  …the bridge can remain an ID-preserving relationship migration.

== Counts
|===
|DB|Table|Rows
|old|public.blog_blogpost_categories|67
|new|public.publications_publication_keywords|67
|===

== Relationship-level semantic migration verification

=== Pair-set preservation (links)
|===
|Check|Result
|Old links (rows)|67
|New links (rows)|67
|Old distinct links|67
|New distinct links|67
|Old pairs missing in new|0
|New pairs not in old|0
|Conclusion|Exact preservation of post↔category relationships
|===

== Referential integrity checks (new)
|===
|Check|Result
|publication_id FK orphans|0
|tagulous_publication_keywords_id FK orphans|0
|Conclusion|New bridge table is referentially clean
|===

== Column model comparison

=== Bridge semantics
|===
|Concept|Old column(s)|New column(s)
|Bridge row id|`id` (int4)|`id` (int8)
|Post FK|`blogpost_id` → `blog_blogpost(id)`|`publication_id` → `publications_publication(id)`
|Category/Tag FK|`blogcategory_id` → `blog_blogcategory(id)`|`tagulous_publication_keywords_id` → `publications_tagulous_publication_keywords(id)`
|===

== Constraints and indexes

=== Uniqueness
Old:

* `UNIQUE (blogpost_id, blogcategory_id)`

New:

* `UNIQUE (publication_id, tagulous_publication_keywords_id)`

Observation:

* Both enforce one link per (post, category/tag) pair.
* This aligns with the observed *0 duplicate pairs*.

== Conclusion
This table is a **direct, lossless migration** of the blog post category links into the new Tagulous-backed keyword relationship table:

* same row count
* identical distinct relationship set
* clean FKs
* consistent with ID preservation in both the post and category/tag base tables
