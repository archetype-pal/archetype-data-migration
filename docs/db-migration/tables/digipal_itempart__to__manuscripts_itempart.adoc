= public.digipal_itempart (old) → public.manuscripts_itempart (new)

== Summary
* Rows: 712 → 713 (all old rows preserved by `id`, plus 1 extra new sentinel row `id = -1`)
* Old IDs are fully preserved (`0` missing); new has one extra operational row (`-1`)
* Shared identity column `id` is preserved exactly; `current_item_id` is preserved exactly for all migrated rows (712/712)
* `custom_label` changed from nullable `varchar(300)` → NOT NULL `varchar(80)` with no length overflow; migration coalesces NULLs to blank string
* New `current_item_locus` is fully populated and matches `COALESCE(old.locus, '')` for all migrated rows (712/712)
* New `historical_item_id` is introduced, fully populated for migrated rows (712/712), and all values resolve to valid FK targets
* Schema redesign removes legacy item-part metadata (`display_label`, `pagination`, `group_id`, `type_id`, `notes`, etc.) and adds normalized `historical_item_id` + `current_item_locus`
* Extra sentinel row (`id=-1`) is intentionally added and is used by `manuscripts_itemimage` (1235 rows), not part of the 1:1 old migration set

== Counts
|===
|DB|Table|Rows
|old|public.digipal_itempart|712
|new|public.manuscripts_itempart|713
|===

== Direct row identity preservation
|===
|Check|Result
|Old IDs missing in new|0
|New IDs not in old|1
|Extra new ID(s)|`-1`
|Conclusion|Complete preservation of all old rows by `id`, plus one extra sentinel/support row in new
|===

== Column model comparison

=== Common columns
|===
|Column|Old|New|Notes
|id|int4 NOT NULL|int8 NOT NULL|type widened; preserved
|current_item_id|int4 NULL|int8 NULL|type widened; preserved
|custom_label|varchar(300) NULL|varchar(80) NOT NULL|length narrowed + nullability tightened; migrated via coalescing NULL→blank
|===

=== Old-only columns (removed in new model)
* `locus`
* `display_label`
* `created`
* `modified`
* `pagination`
* `group_id` (self-FK)
* `type_id` (FK to `digipal_itemparttype`)
* `group_locus`
* `notes`
* `keywords_string`

=== New-only columns
* `historical_item_id` (FK to `manuscripts_historicalitem`, NOT NULL)
* `current_item_locus` (varchar(30), NOT NULL)

== Constraints and indexes

=== Constraints
Old:

* Primary key on `id`
* FK `current_item_id` → `digipal_currentitem(id)`
* FK `group_id` → `digipal_itempart(id)`
* FK `type_id` → `digipal_itemparttype(id)`

New:

* Primary key on `id`
* FK `current_item_id` → `manuscripts_currentitem(id)`
* FK `historical_item_id` → `manuscripts_historicalitem(id)`

Observation:

* Legacy grouping/type relations (`group_id`, `type_id`) are removed.
* New schema introduces normalized mandatory `historical_item_id`.

=== Indexes
Old:

* PK index
* index on `current_item_id`
* index on `group_id`
* index on `type_id`

New:

* PK index
* index on `current_item_id`
* index on `historical_item_id`

== Data quality / blocker checks (generic same-column checks)
|===
|Check|Result
|Old NULLs in common new NOT NULL column `id`|0
|Old NULLs in common new NOT NULL column `custom_label`|704
|Old values exceeding new varchar length for `custom_label` (80)|0
|===

Interpretation:

* `custom_label` cannot be copied directly because most old rows are NULL (704/712), while new is NOT NULL.
* No truncation blocker exists (`old_too_long = 0`), so NULL→blank coalescing is sufficient.

== Semantic migration verification

=== Comparable payload subset (excluding sentinel row) is an exact multiset match
For the derivable subset:

* `current_item_id`
* `current_item_locus` (derived as `COALESCE(old.locus, '')`)
* `custom_label` (derived as `COALESCE(old.custom_label, '')`)

Duplicate profile:
|===
|Check|Result
|Old projected duplicate subset rows|0
|New subset duplicate rows (excluding `id=-1`)|0
|===

Set comparison:
|===
|Check|Result
|Old projected rows missing in new subset|0
|New subset rows not in old projected|0
|Conclusion|Exact multiset equality on the derivable payload subset (excluding sentinel row)
|===

=== Row-level semantic check by `id` (712 migrated rows)
|===
|Check|Result
|Total joined by `id`|712
|`current_item_id` same|712
|`current_item_id` changed|0
|`current_item_locus = COALESCE(old.locus,'')`|712
|`custom_label = COALESCE(old.custom_label,'')`|712
|`new.historical_item_id` NULLs|0
|`new.historical_item_id` unresolved FK|0
|`new.historical_item_id` resolved FK|712
|===

Conclusion:

* All migrated rows preserve `current_item_id`.
* `current_item_locus` and `custom_label` are exact coalesced transformations from old values.
* New mandatory `historical_item_id` is fully and validly populated for all migrated rows.

== Legacy field profiling and dropped-field interpretation

=== Old table profile (selected fields)
|===
|Field|Observation
|`locus` NULLs|0
|`locus` blank-or-null|712 (all rows effectively blank)
|`display_label` blank-or-null|0 (always populated)
|`custom_label` NULLs|704
|`custom_label` blank-or-null|712
|`pagination` true/false|0 / 712 (always false)
|`group_id` NULLs / non-NULLs|712 / 0
|`type_id` NULLs / non-NULLs|712 / 0
|`group_locus` blank-or-null|712
|`notes` blank-or-null|710
|`keywords_string` blank-or-null|88
|===

Interpretation:

* Several legacy fields are unused or effectively empty (`locus`, `group_id`, `type_id`, `group_locus`, mostly `notes`, mostly `custom_label`).
* The new model intentionally simplifies item-part representation while enforcing normalized links.

=== Legacy FK integrity and new FK integrity
|===
|Check|Result
|Old `current_item_id` FK orphans|0
|Old `group_id` FK orphans|0
|Old `type_id` FK orphans|0
|New `current_item_id` FK orphans|0
|New `historical_item_id` FK orphans|0
|===

== Relationship usage preservation and sentinel row

=== Usage counts for migrated IDs
|===
|Check|Result
|Itempart IDs compared|712
|Usage count same|712
|Usage count diff|0
|===

Interpretation:

* For the 712 migrated IDs, downstream usage counts are preserved exactly.

=== Extra new sentinel row (`id = -1`)
Observed extra row:

* `id = -1`
* `current_item_id = 2`
* `historical_item_id = 1`
* `current_item_locus = "-public.manuscripts_itemimage"`
* `custom_label = "Created for all the nulls contained in public.digipal_image "`

And usage:

* Referenced by `manuscripts_itemimage`: `1235` rows

Interpretation:

* This is an intentional support/sentinel row added during migration to absorb null/legacy edge cases in image references.
* It explains the row-count increase (712 → 713) without indicating data loss.

== Compact migration summary (all-row view)
|===
|Check|Result
|Old total|712
|New total|713
|Joined old→new by `id`|712
|Extra new rows|1
|Extra new row is `id=-1`|1
|`current_item_id` preserved (joined rows)|712
|`current_item_locus` matches coalesced old `locus`|712
|`custom_label` matches coalesced old `custom_label`|712
|New `historical_item_id` NULLs (joined rows)|0
|New `current_item_locus` blank-or-null (joined rows)|712
|New `custom_label` blank-or-null (joined rows)|712
|===

== Conclusion
This is a complete migration of `digipal_itempart` into `manuscripts_itempart` with one intentional extra support row:

* all 712 legacy rows are preserved by `id`
* shared relational identity (`current_item_id`) is preserved exactly
* new payload fields are deterministic transformations:
** `current_item_locus = COALESCE(old.locus, '')`
** `custom_label = COALESCE(old.custom_label, '')`
* new `historical_item_id` is fully populated and FK-valid for all migrated rows
* legacy grouping/type metadata and audit/text fields are dropped as part of schema simplification
* one extra sentinel row (`id=-1`) is deliberately added for downstream image migration edge cases
