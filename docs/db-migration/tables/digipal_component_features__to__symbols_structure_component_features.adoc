= public.digipal_component_features (old) → public.symbols_structure_component_features (new)

== Summary
* Rows: 76 → 76 (complete transfer; IDs preserved)
* 1:1 migration by `id` (no missing old IDs; no extra new IDs)
* Exact preservation of the relationship payload by `id`: `component_id` and `feature_id` both match for all 76 rows
* Exact pair-set preservation of the M2M relation: no `(component_id, feature_id)` pairs missing or added
* New schema keeps the same logical model (through table with PK + FK pair + unique pair), with renamed FK targets in the new app namespace
* Legacy `set_by_default` was dropped; profiling shows it was always `FALSE` (76/76), so removal is low-risk and loss-minimal
* Audit fields (`created`, `modified`) removed in the new model

== Counts
|===
|DB|Table|Rows
|old|public.digipal_component_features|76
|new|public.symbols_structure_component_features|76
|===

== Direct row identity preservation
|===
|Check|Result
|Old IDs missing in new|0
|New IDs not in old|0
|Conclusion|Complete 1:1 migration by `id`
|===

== Column model comparison

=== Common columns
|===
|Column|Old|New|Notes
|id|int4 NOT NULL|int8 NOT NULL|type widened
|component_id|int4 NOT NULL|int8 NOT NULL|type widened; FK target renamed to new schema/app
|feature_id|int4 NOT NULL|int8 NOT NULL|type widened; FK target renamed to new schema/app
|===

=== Old-only columns (removed in new model)
* `set_by_default` (boolean, NOT NULL)
* `created`
* `modified`

=== New-only columns
* none

== Constraints and indexes

=== Constraints
Old:

* Primary key on `id`
* Unique constraint on `(component_id, feature_id)`
* FK `component_id` → `digipal_component(id)`
* FK `feature_id` → `digipal_feature(id)`

New:

* Primary key on `id`
* Unique constraint on `(component_id, feature_id)`
* FK `component_id` → `symbols_structure_component(id)`
* FK `feature_id` → `symbols_structure_feature(id)`

Observation:

* Constraint names changed (expected Django/app rename effect), but the logical constraint model is preserved.
* This remains a canonical through / join table in the new schema.

=== Indexes
Old:

* PK index
* unique composite index on `(component_id, feature_id)`
* index on `component_id`
* index on `feature_id`

New:

* PK index
* unique composite index on `(component_id, feature_id)`
* index on `component_id`
* index on `feature_id`

Observation:

* Indexing strategy is functionally preserved.

== Data quality / blocker checks (generic same-column checks)
|===
|Check|Result
|Old NULLs in common new NOT NULL column `id`|0
|Old NULLs in common new NOT NULL column `component_id`|0
|Old NULLs in common new NOT NULL column `feature_id`|0
|Varchar overflow blockers on shared columns|None applicable (no shared varchar columns)
|===

Interpretation:

* No blockers for direct transfer of shared columns (`id`, `component_id`, `feature_id`).

== Semantic migration verification (through-table relation preservation)

=== Pair uniqueness in new table
Duplicate-check query on `(component_id, feature_id)` returned no rows.

|===
|Check|Result
|Duplicate `(component_id, feature_id)` rows in new table|0 (no duplicate groups returned)
|===

Conclusion:

* New data respects the intended unique-pair semantics.

=== Exact pair-set preservation (relationship equivalence)
|===
|Check|Result
|Old `(component_id, feature_id)` pairs missing in new|0
|New `(component_id, feature_id)` pairs not in old|0
|Conclusion|Exact pair-set preservation
|===

Interpretation:

* The many-to-many relationship assignments were migrated exactly, with no dropped or extra links.

=== By-`id` FK payload preservation
Verified by joining old/new rows on `id`.

|===
|Check|Result
|Total joined rows|76
|Rows where `component_id` matches exactly|76
|Rows where `component_id` changed|0
|Rows where `feature_id` matches exactly|76
|Rows where `feature_id` changed|0
|===

Conclusion:

* This is not only pair-set equivalent; it is an exact by-row (`id`) carry-over of the join relationships.

=== New FK integrity checks
|===
|Check|Result
|`component_id` FK orphans in new|0
|`feature_id` FK orphans in new|0
|===

Interpretation:

* All migrated join rows reference valid parent rows in the new schema.

== Dropped field profile (old-only `set_by_default`)
Profiling of the removed old column:

|===
|Check|Result
|Total rows profiled|76
|`set_by_default = TRUE`|0
|`set_by_default = FALSE`|76
|===

Interpretation:

* `set_by_default` contained no positive/active values in the legacy data.
* Dropping this column in the new schema does not remove any observed non-default state.

== Structural comparison details (evidence recap)

=== Column order / shape
Old columns:

* `id`
* `component_id`
* `feature_id`
* `set_by_default`
* `created`
* `modified`

New columns:

* `id`
* `component_id`
* `feature_id`

Observation:

* New model is a leaner join table retaining only the relational payload.

=== FK definitions (logical equivalence)
Old:

* `component_id` → `digipal_component(id)`
* `feature_id` → `digipal_feature(id)`

New:

* `component_id` → `symbols_structure_component(id)`
* `feature_id` → `symbols_structure_feature(id)`

Interpretation:

* This is a namespace/model migration of referenced entities, with the join semantics preserved.

== Conclusion
This table is a clean, exact migration of a legacy through-table into the new symbols schema:

* `id`, `component_id`, and `feature_id` are preserved exactly
* the join relationship set is preserved exactly (no missing/extra pairs)
* uniqueness and FK integrity are preserved in the new schema
* the only dropped payload column (`set_by_default`) was always `FALSE` in the legacy data
* audit metadata (`created`, `modified`) was intentionally removed as part of schema simplification
