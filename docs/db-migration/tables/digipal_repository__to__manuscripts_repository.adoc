= public.digipal_repository (old) → public.manuscripts_repository (new)

== Summary
* Rows: 9 → 9 (complete transfer; IDs preserved)
* 1:1 migration by `id` (no missing old IDs; no extra new IDs)
* `name` is an exact carry-over (9/9)
* `url` is semantically preserved (9/9 trim/casefold match; 3 NULLs on both sides)
* `name` length reduced from `varchar(256)` to `varchar(100)` with no overflow/truncation blockers
* `url` remains `varchar(200)` with no overflow/truncation blockers
* New `place` is a fully materialized string and matches legacy `digipal_place.name` exactly (9/9)
* New `type` is a fully materialized string and matches legacy `digipal_ownertype.name` exactly (9/9)
* New `label` is a curated abbreviation field: exact carry-over from old `short_name` in 6/9 rows, with 3/9 additional labels populated where old `short_name` was blank (e.g. `CAM`, `PMFED`, `LTNA`)
* Major schema redesign: legacy FKs / flags / permissions / hierarchy metadata removed or flattened into in-row strings (`place`, `type`, `label`)
* Repository usage via related item FK is preserved (same used IDs and same per-repository usage counts)

== Counts
|===
|DB|Table|Rows
|old|public.digipal_repository|9
|new|public.manuscripts_repository|9
|===

== Direct row identity preservation
|===
|Check|Result
|Old IDs missing in new|0
|New IDs not in old|0
|Conclusion|Complete 1:1 migration by `id`
|===

== Name identity and duplicate-name preservation
|===
|Check|Result
|Rows compared by `id`|9
|`new.name = old.name` exact|9
|`name` differences|0
|Trimmed-name matches|9
|Case-folded-name matches|9
|Duplicate `name` rows in old|1 duplicated value (`The National Archives` appears 2 times)
|Duplicate `name` rows in new|1 duplicated value (`The National Archives` appears 2 times)
|Name-set parity (`old_names_missing_in_new`, `new_names_not_in_old`)|0 / 0
|===

Interpretation:

* `name` is preserved exactly by `id`.
* Non-unique names are intentionally preserved (this is not a unique-name table).

== Column model comparison

=== Common columns
|===
|Column|Old|New|Notes
|id|int4 NOT NULL|int8 NOT NULL|type widened
|name|varchar(256) NOT NULL|varchar(100) NOT NULL|exactly preserved; length narrowed safely
|url|varchar(200) NULL|varchar(200) NULL|semantically preserved
|===

=== Old-only columns (removed / flattened in new model)
* `legacy_id`
* `short_name`
* `place_id` (FK to `digipal_place`)
* `comma`
* `british_isles`
* `digital_project`
* `created`
* `modified`
* `copyright_notice`
* `media_permission_id` (FK)
* `type_id` (FK to `digipal_ownertype`)
* `part_of_id` (self-FK hierarchy)

=== New-only columns
* `label` (`varchar(64)`, nullable in schema; fully populated in data)
* `place` (`varchar(50)`, NOT NULL) — materialized from legacy place lookup
* `type` (`varchar(30)`, nullable in schema; fully populated in data) — materialized from legacy owner-type lookup

== Constraints and indexes

=== Constraints
Old:

* Primary key on `id`
* FK `place_id` → `digipal_place(id)`
* FK `type_id` → `digipal_ownertype(id)`
* FK `media_permission_id` → `digipal_mediapermission(id)`
* FK `part_of_id` → `digipal_repository(id)` (self-reference)

New:

* Primary key on `id`

Observation:

* The new schema removes all legacy FK dependencies and stores selected lookup values directly as strings (`place`, `type`).
* Legacy repository hierarchy (`part_of_id`) is not represented in the new table.

=== Indexes
Old:

* PK index
* index on `place_id`
* index on `type_id`
* index on `media_permission_id`
* index on `part_of_id`

New:

* PK index only

== Data quality / blocker checks (generic same-column checks)
|===
|Check|Result
|Old NULLs in common new NOT NULL column `id`|0
|Old NULLs in common new NOT NULL column `name`|0
|Old values exceeding new varchar length for `name` (100)|0
|Old values exceeding new varchar length for `url` (200)|0
|===

Interpretation:

* No blockers for transfer of shared columns (`id`, `name`, `url`).

== Semantic migration verification

=== `name` exact carry-over
Verified by joining old/new rows on `id`.

|===
|Check|Result
|Total rows compared|9
|Rows where `new.name = old.name`|9
|Rows where `new.name` differs|0
|Trimmed-name matches|9
|Case-folded matches|9
|===

Conclusion:

* `manuscripts_repository.name` is an exact semantic carry-over of `digipal_repository.name`.

=== `url` semantic preservation
|===
|Check|Result
|Total rows|9
|Old `url` NULLs|3
|New `url` NULLs|3
|Exact `=` matches|6
|Trimmed matches (NULL-safe coalesce comparison)|9
|Case-folded matches (NULL-safe)|9
|===

Interpretation:

* `url` is preserved exactly, including NULL distribution.
* The `6` exact matches reflect non-NULL rows only; the remaining 3 are NULL on both sides.

=== `new.place` derives from legacy place lookup (`digipal_place.name`)
Old FK-resolution sanity:
|===
|Check|Result
|Total old rows|9
|`old.place_id` NULLs|0
|Unresolved `place_id` FKs after join to `digipal_place`|0
|===

Comparison (`new.place` vs joined `old_place_name`):
|===
|Check|Result
|Total rows|9
|`new.place` NULLs|0
|`new.place` blank-or-null|0
|`old_place_name` NULLs|0
|Exact matches (`new.place = old_place_name`)|9
|Trimmed matches|9
|Case-folded matches|9
|===

Conclusion:

* `manuscripts_repository.place` is an exact materialization of legacy `digipal_repository.place_id → digipal_place.name`.

=== `new.type` derives from legacy owner-type lookup (`digipal_ownertype.name`)
Old FK-resolution sanity:
|===
|Check|Result
|`old.type_id` NULLs|0
|Unresolved `type_id` FKs after join to `digipal_ownertype`|0
|===

Comparison (`new.type` vs joined `old_type_name`):
|===
|Check|Result
|Total rows|9
|`new.type` NULLs|0
|`new.type` blank-or-null|0
|`old_type_name` NULLs|0
|Exact non-NULL matches|9
|Case-folded matches (including null logic)|9
|Old type NULL → new type NULL|0
|Old type NULL → new type non-NULL|0
|Old type non-NULL → new type NULL|0
|===

Observed distribution:

* `Library` → `Library` = 7
* `Unknown` → `Unknown` = 2

Conclusion:

* `manuscripts_repository.type` is an exact materialization of legacy `digipal_repository.type_id → digipal_ownertype.name`.

=== `new.label` derivation evidence (legacy `short_name` + curated backfill)
Profiling and candidate checks:
|===
|Check|Result
|Total rows|9
|`new.label` NULLs|0
|`new.label` blank-or-null|0
|Old `short_name` NULLs|3
|Old `short_name` blank-or-null|3
|`new.label = old.short_name` exact|6
|`new.label = old.short_name` trim/casefold|6
|`new.label = old.name` exact|0
|`new.label = coalesce(NULLIF(old.short_name,''), old.name)`|6
|===

Row-level evidence:

* 6 rows match old `short_name` exactly (`NRS`, `TNA`, `BL`, `NLS`, `DCA`, `TNA (not DL)`)
* 3 rows have blank old `short_name` but populated new `label`:
** id `3`: `Charters of the Abbey of Melrose` → `CAM`
** id `8`: `Papers of the Maule Family, Earls of Dalhousie` → `PMFED`
** id `11`: `London, The National Archives` → `LTNA`

Interpretation:

* `label` is not simply copied from `name`.
* `label` is populated from old `short_name` where available.
* Where old `short_name` is blank, `label` has been curated/backfilled (likely acronym/abbreviation generation or manual mapping).

Strong evidence supports:

* `manuscripts_repository.label` is a normalized shorthand/abbreviation field sourced primarily from `short_name`, with additional migration enrichment for missing legacy values.

== Legacy dropped field profile (old table)
|===
|Field / check|Observation
|`short_name` NULLs|3 / 9
|`part_of_id` non-NULL|1 / 9
|`media_permission_id` non-NULL|2 / 9
|`type_id` non-NULL|9 / 9
|`comma` TRUE / FALSE / NULL|0 / 0 / 9
|`british_isles` TRUE / FALSE / NULL|3 / 0 / 6
|`digital_project` TRUE / FALSE / NULL|0 / 0 / 9
|===

Interpretation:

* Several legacy boolean/permission fields were sparse or mostly NULL and are omitted in the new simplified model.
* `type_id` and `place_id` were not dropped semantically; they were flattened into `type` and `place`.

== Legacy hierarchy (`part_of_id`) note
Old self-FK profile:
|===
|Check|Result
|Total rows|9
|`part_of_id` NULLs|8
|`part_of_id` non-NULL|1
|Hierarchy FK orphans|0
|===

Observed edge:

* `British Library` (id `5`) has `part_of_id = 5` (self-reference)

Interpretation:

* The only legacy hierarchy edge is a self-loop, and hierarchy is not represented in the new schema.

== Usage preservation (related item FK usage)
Checks performed on repository usage in related item table(s):

|===
|Check|Result
|Old repository FK orphans in related items|0
|New repository FK orphans in related items|0
|Repository IDs compared for usage counts|5
|IDs with same usage count old vs new|5
|IDs with different usage count|0
|===

Population summary:
|===
|Metric|Old|New
|Total repositories|9|9
|Used repositories|5|5
|Unused repositories|4|4
|===

Conclusion:

* Repository references were preserved consistently in dependent data, not just in the repository table itself.

== Example evidence (sample-based)
Representative rows from the semantic proof sheet (`id`-joined old/new):

* `id=2` — `place_id=4 → Edinburgh`, new `place='Edinburgh'`; `short_name='NRS'`, new `label='NRS'`
* `id=3` — old `short_name` blank, new `label='CAM'`; `place_id=3 → Unknown`, new `place='Unknown'`; `type='Unknown'`
* `id=5` — `url='https://www.bl.uk/'` preserved exactly; `place='London'`; `type='Library'`; self `part_of_id=5` in old only
* `id=11` — old `short_name` blank, new `label='LTNA'`; `place='London, The National Archives'` materialized from place lookup

== Conclusion
This table is a complete 1:1 entity migration with exact identity preservation and a clear flattening/normalization redesign:

* `id` and `name` are preserved exactly
* `url` is preserved (including NULL distribution)
* legacy FK-backed values are materialized as strings:
** `place_id` → `place` (exact lookup-name carry-over)
** `type_id` → `type` (exact lookup-name carry-over)
* new `label` is a fully populated shorthand field, mostly sourced from `short_name` with additional curated backfill where legacy `short_name` was missing
* legacy permissions/flags/audit metadata and self-hierarchy fields are removed from the new model
* downstream repository usage counts remain consistent across old and new datasets
