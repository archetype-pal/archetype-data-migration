= public.digipal_hand_images (old) → public.scribes_hand_item_part_images (new)

== Summary
* Rows: 715 → 715 (complete transfer; IDs preserved)
* Through-table semantics preserved exactly
* Schema rename/remodel: `image_id` → `itemimage_id`
* FKs retargeted from old Digipal tables to new `scribes` / `manuscripts` tables

== Counts
|===
|DB|Table|Rows
|old|public.digipal_hand_images|715
|new|public.scribes_hand_item_part_images|715
|===

== Column changes
|===
|Column|Old|New|Notes
|id|int4 NOT NULL|int8 NOT NULL|type widened
|hand_id|int4 NOT NULL|int8 NOT NULL|type widened; FK retargeted to `scribes_hand`
|image_id|int4 NOT NULL|—|replaced/renamed in new schema
|itemimage_id|—|int8 NOT NULL|NEW name for image FK; points to `manuscripts_itemimage`
|===

== Constraints and indexes
Old:

* FK `hand_id` → `digipal_hand(id)`
* FK `image_id` → `digipal_image(id)`
* UNIQUE (`hand_id`, `image_id`)
* Indexes on `hand_id`, `image_id`, and PK (`id`)

New:

* FK `hand_id` → `scribes_hand(id)`
* FK `itemimage_id` → `manuscripts_itemimage(id)`
* UNIQUE (`hand_id`, `itemimage_id`)
* Indexes on `hand_id`, `itemimage_id`, and PK (`id`)

== Data quality / blocker checks
|===
|Check|Result
|Old NULLs in common new NOT NULL columns (`id`, `hand_id`)|0
|Old values exceeding new varchar lengths (common string columns)|Not applicable / none detected
|Missing IDs after migration|0
|===

== Mapping verification
Row-by-row verification using `id`:
|===
|Check|Result
|Joined rows by `id`|715
|Rows with same `hand_id`|715
|Rows with changed `hand_id`|0
|Rows where `new.itemimage_id = old.image_id`|715
|Rows where `new.itemimage_id != old.image_id`|0
|===

Pair-set equivalence verification:
|===
|Check|Result
|Old (`hand_id`,`image_id`) pairs missing in new (`hand_id`,`itemimage_id`)|0
|New (`hand_id`,`itemimage_id`) pairs not present in old (`hand_id`,`image_id`)|0
|===

== Conclusion
This table migrated completely and exactly in relational terms.
It is a through-table migration with:

* preserved row count
* preserved surrogate IDs
* preserved join pairs
* preserved uniqueness semantics
* FK retargeting to the new schema (`digipal_image` → `manuscripts_itemimage`, `digipal_hand` → `scribes_hand`)
