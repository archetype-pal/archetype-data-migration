= public.digipal_source (old) → public.manuscripts_bibliographicsource (new)

== Summary
* Rows: 40 → 40 (counts match)
* IDs preserved: all 40 rows join 1:1 by `id` (no missing/extra IDs)
* `name` preserved exactly: 40/40 exact matches by `id`
* Schema simplified:
** removed legacy/admin fields: `created`, `modified`, `priority`, `label_styled`, `label_slug`
** widened types: `id int4 → int8`, `name varchar(128) → varchar(200)`, `label varchar(30) → varchar(100)`
** changed nullability: `label` becomes **NOT NULL** in new
* Constraints/indexes diverge:
** old enforced uniqueness on `name` and `label`
** new currently only enforces primary key on `id` (no unique constraints on `name`/`label`)
* Relationship context preserved (by FK targets):
** old referenced by: `digipal_cataloguenumber`, `digipal_description`, `digipal_handdescription`, `digipal_itempartauthenticity`
** new referenced by: `manuscripts_cataloguenumber.catalogue_id`, `manuscripts_historicalitemdescription.source_id`

== Counts
|===
|DB|Table|Rows
|old|public.digipal_source|40
|new|public.manuscripts_bibliographicsource|40
|===

== Direct row identity preservation
|===
|Check|Result
|Old IDs missing in new|0
|New IDs not in old|0
|Joined rows by `id` (old↔new)|40
|Conclusion|Full 1:1 row migration by `id`
|===

== Name preservation (joined by `id`)
|===
|Check|Result
|`name` exact match|40 / 40
|`name` normalized match (trim+casefold)|40 / 40
|Old duplicate names (normalized)|0
|New duplicate names (normalized)|0
|===

Interpretation:

* `name` is a stable application key here and appears to have been migrated without change.

== Column model comparison

=== Common columns
|===
|Column|Old|New|Notes
|id|int4 NOT NULL|int8 NOT NULL|type widened
|name|varchar(128) NOT NULL|varchar(200) NOT NULL|length widened
|label|varchar(30) NULL|varchar(100) NOT NULL|length widened; nullability tightened
|===

=== Old-only columns (removed in new model)
* `created` (timestamptz NOT NULL)
* `modified` (timestamptz NOT NULL)
* `priority` (int4 NOT NULL)
* `label_styled` (varchar(30) NULL)
* `label_slug` (varchar(30) NULL)

=== New-only columns
* None

== Constraints and indexes

=== Constraints
Old:

* Primary key on `id`
* Unique constraint on `name`
* Unique constraint on `label`

New:

* Primary key on `id`

Observation:

* The new table currently does **not** enforce uniqueness of `name` or `label`.
* If uniqueness is still required at application level (as in the old schema), consider re-adding unique constraints (or confirm the new application layer handles collisions intentionally).

=== Indexes
Old:

* PK index on `id`
* unique index on `name` + pattern-ops index on `name`
* unique index on `label` + pattern-ops index on `label`
* indexes on `label_slug` + pattern-ops index on `label_slug`

New:

* PK index on `id` only

== Data quality / blocker checks

=== Old NULLs that could violate new NOT NULL (shared columns)
|===
|Column|Old NULLs|New is NOT NULL?
|id|0|Yes
|name|0|Yes
|label|_(not measured in this run)_|Yes
|===

Notes:

* Old `label` was nullable, while new `label` is NOT NULL.
* The migration succeeded in producing 40 rows in new; however, if old contained NULL labels, they must have been populated/derived during migration (e.g., from `name` or another field). If you want to confirm, run a quick old-side check:

[source,sql]
----
SELECT count(*) AS old_label_nulls
FROM dblink('old','SELECT label IS NULL FROM public.digipal_source') AS t(is_null boolean)
WHERE is_null;
----

=== Varchar length fit
|===
|Column|Old max observed|New limit|Blocker?
|name|48|200|No
|label|_(not measured in this run)_|100|No evidence of overflow
|===

== Referential integrity and usage context

=== New inbound FKs to `manuscripts_bibliographicsource`
|===
|Referencing table|Column|Meaning
|manuscripts_cataloguenumber|catalogue_id|bibliographic source used by catalogue numbers
|manuscripts_historicalitemdescription|source_id|bibliographic source used by historical item descriptions
|===

=== Old inbound FKs to `digipal_source`
|===
|Referencing table|Column|Meaning
|digipal_cataloguenumber|source_id|catalogue number source
|digipal_description|source_id|description source
|digipal_handdescription|source_id|hand description source
|digipal_itempartauthenticity|source_id|authenticity source
|===

Interpretation:

* This table is a **shared lookup/reference** that supports multiple domain tables.
* Given ID preservation and exact `name` preservation, downstream migrated FKs (e.g., `digipal_cataloguenumber → manuscripts_cataloguenumber`) should remain consistent.

== Conclusion
`digipal_source → manuscripts_bibliographicsource` is a **clean 1:1 lookup migration**:

* row counts match and IDs are fully preserved
* `name` is preserved exactly for all rows
* schema is simplified by dropping legacy metadata fields
* `label` is strengthened in the new schema (NOT NULL, longer), but uniqueness constraints present in the old schema are not present in new—confirm whether that is intentional or should be reinstated for integrity.
