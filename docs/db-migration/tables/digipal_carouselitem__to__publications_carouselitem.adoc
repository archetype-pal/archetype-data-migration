= public.digipal_carouselitem (old) → public.publications_carouselitem (new)

== Summary
* Rows: 8 → 8 (complete transfer; IDs preserved)
* 1:1 migration by `id` (no missing old IDs; no extra new IDs)
* `ordering` is derived from old `sort_order` (8/8)
* `url` is derived from old `link` (8/8; exact + trim match)
* `image` is mapped via path normalization:
** old `/media/uploads/Carousel/...` → new `/media/carousel/...` (8/8)
* `title` is mostly preserved (7/8 exact), with one row cleaned/shortened:
** old title length reduced from varchar(300) → varchar(150) with 1 overflow case (the cleaned row)
* Legacy audit/metadata columns removed (`created`, `modified`, `image_alt`, `image_title`, `image_file`)
* New schema adds a check constraint `ordering >= 0`

== Counts
|===
|DB|Table|Rows
|old|public.digipal_carouselitem|8
|new|public.publications_carouselitem|8
|===

== Direct row identity preservation
|===
|Check|Result
|Old IDs missing in new|0
|New IDs not in old|0
|Joined rows by `id`|8
|Joined rows where `ordering = old.sort_order`|8
|Joined rows where `url = old.link`|8
|Joined rows where `image = normalize(old.image)`|8
|Joined rows where `title` exact|7
|Conclusion|Complete 1:1 migration by `id`; one title row transformed during migration
|===

== Column model comparison

=== Common columns
|===
|Column|Old|New|Notes
|id|int4 NOT NULL|int8 NOT NULL|type widened
|image|varchar(200) NULL|varchar(100) NOT NULL|populated in old (0 NULLs); path normalized; length narrowed safely
|title|varchar(300) NOT NULL|varchar(150) NOT NULL|7/8 exact; 1 cleaned/shortened; length narrowed with 1 old-too-long case
|===

=== Old-only columns (removed in new model)
* `link` (varchar(200)) — migrated into `url`
* `sort_order` (int4) — migrated into `ordering`
* `created`, `modified`
* `image_alt`, `image_title`, `image_file`

=== New-only columns
* `ordering` (int4, NOT NULL)
* `url` (varchar(200), NOT NULL)

== Constraints and indexes

=== Constraints
Old:

* Primary key on `id`

New:

* Primary key on `id`
* Check constraint `ordering >= 0`

== Data quality / blocker checks (generic)
|===
|Check|Result
|Old NULLs in shared new NOT NULL columns (`id`, `image`, `title`)|0 across all checked columns
|Old values exceeding new varchar length for `image` (100)|0
|Old values exceeding new varchar length for `title` (150)|1
|===

== Semantic migration verification

=== Ordering and URL derivation
Joined by `id`:

|===
|Check|Result
|`ordering = old.sort_order`|8/8
|`url = old.link` (exact)|8/8
|`url = trim(old.link)`|8/8
|===

=== Image path normalization
Joined by `id`:

|===
|Check|Result
|`new.image = old.image` (exact)|0/8
|`new.image = replace(old.image, '/media/uploads/Carousel/', '/media/carousel/')`|8/8
|===

=== Title cleaning hypothesis
Comparisons on the joined rows:

|===
|Check|Result
|`new.title = old.title` (exact)|7/8
|`new.title = strip_html(old.title)`|6/8
|`new.title = first_sentence(strip_html(old.title))`|7/8
|===

=== Evidence row (the transformed title)
|===
|Field|Value
|id|5
|old.title|About Models of Authority.</a> <span style="font-size: 75%">Detail from <a href="http://digital.nls.uk/scotlandspages/timeline/1159.html">Kelso Charter</a> reproduced by permission of His Grace The Duke of Roxburghe</span>
|new.title|About Models of Authority
|old.image|/media/uploads/Carousel/kelso_image.jpg
|new.image|/media/carousel/kelso_image.jpg
|old.link|/about/
|new.url|/about/
|old.sort_order|1
|new.ordering|1
|===

== Conclusion
`publications_carouselitem` is a full 1:1 migration of the legacy carousel items with normalization:

* IDs, ordering, URL, and image relationships are preserved (with systematic image path rewriting)
* one overly-long/HTML-heavy title is cleaned to fit the new schema (`varchar(150)`)
* legacy audit and presentation metadata are dropped in favor of a simplified carousel model
