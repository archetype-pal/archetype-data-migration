= public.digipal_script (old) → public.scribes_script (new)

== Summary
* Rows: 0 → 0 (no data in either environment)
* This is a *schema-only* compatibility pair: the table exists in both databases, but contains no rows to migrate.
* Schema refactor:
** old drops `legacy_id`, `created`, `modified`
** `id` is widened int4 → int8
** `name` shrinks varchar(128) → varchar(100)
* Because there are no rows, data-preservation checks are vacuously satisfied.

== Counts
|===
|DB|Table|Rows
|old|public.digipal_script|0
|new|public.scribes_script|0
|===

== Column model comparison

=== Old columns
|===
|Column|Type|Nullable|Notes
|id|int4|NO|sequence-backed in old
|legacy_id|int4|YES|legacy metadata
|name|varchar(128)|NO|script name
|created|timestamptz|NO|audit field
|modified|timestamptz|NO|audit field
|===

=== New columns
|===
|Column|Type|Nullable|Notes
|id|int8|NO|widened identifier
|name|varchar(100)|NO|shrunk length
|===

== Constraints and indexes

=== Constraints
Old:

* Primary key on `id` (confirmed previously as `digipal_script_pkey` in earlier runs)

New:

* Primary key on `id` (`scribes_script_pkey`)

== Blocker checks
No blockers can be triggered because the old table contains zero rows. If data is inserted in old in the future, the only potential “fit” constraint to watch is:

* `name` length: 128 → 100 (ensure old names do not exceed 100 characters)

== Conclusion
No migration work is required for this pair in the current dataset.
Document it as a **schema-aligned empty table** (0 → 0), with a simplified new model.
