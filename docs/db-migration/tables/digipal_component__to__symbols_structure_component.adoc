= public.digipal_component (old) → public.symbols_structure_component (new)

== Summary
* Rows: 15 → 15 (complete transfer; IDs preserved)
* Exact 1:1 migration by `id` (no missing old IDs; no extra new IDs)
* `name` is an exact carry-over (15/15 by `id`)
* `name` length reduced from varchar(128) to varchar(100) with no overflow/truncation blockers
* Schema is a clean simplification: legacy audit fields (`created`, `modified`) removed
* No table-level FK dependencies in either old/new component table
* Strong relational preservation evidence via bridge table `component_features`: component usage counts match exactly (12 used / 3 unused in both old and new)

== Counts
|===
|DB|Table|Rows

|old
|public.digipal_component
|15

|new
|public.symbols_structure_component
|15
|===

== Direct row identity preservation
|===
|Check|Result

|Old IDs missing in new
|0

|New IDs not in old
|0

|Old `(id,name)` pairs missing in new
|0

|New `(id,name)` pairs not in old
|0

|Conclusion
|Complete 1:1 migration by `id`, with exact `name` preservation
|===

== Column model comparison

=== Common columns
|===
|Column|Old|New|Notes

|id
|int4 NOT NULL
|int8 NOT NULL
|type widened

|name
|varchar(128) NOT NULL
|varchar(100) NOT NULL
|exactly preserved; length narrowed safely
|===

=== Old-only columns (removed in new model)
* `created`
* `modified`

=== New-only columns
* None

== Constraints and indexes

=== Constraints
Old:

* Primary key on `id`
* Unique constraint on `name`

New:

* Primary key on `id`
* Unique constraint on `name`

Observation:

* Core identity and uniqueness semantics are preserved in the new schema (`PK(id)`, `UNIQUE(name)`).
* No foreign keys exist on this base table in either schema (constraint FK comparison is empty).

=== Indexes
Old:

* PK index on `id`
* Unique index on `name`
* Pattern index on `name` (`varchar_pattern_ops`)

New:

* PK index on `id`
* Unique index on `name`
* Pattern index on `name` (`varchar_pattern_ops`)

Observation:

* Indexing strategy is functionally preserved; only generated index names differ.

== Data quality / blocker checks (generic same-column checks)
|===
|Check|Result

|Old NULLs in common new NOT NULL column `id`
|0

|Old NULLs in common new NOT NULL column `name`
|0

|Old values exceeding new varchar length for `name` (100)
|0
|===

Interpretation:

* No blockers for direct transfer of shared columns (`id`, `name`).

== Semantic migration verification

=== `name` exact carry-over by `id`
Verified by joining old/new rows on `id`.

|===
|Check|Result

|Total rows compared
|15

|Rows where `new.name = old.name`
|15

|Rows where `new.name` differs
|0

|Trimmed-name matches
|15

|Case-folded matches
|15
|===

Conclusion:

* `symbols_structure_component.name` is an exact semantic carry-over of `digipal_component.name`.

=== Name uniqueness / set equality checks
|===
|Check|Result

|Old duplicate names
|0 rows (empty result)

|New duplicate names
|0 rows (empty result)

|Old names missing in new
|0

|New names not in old
|0
|===

Conclusion:

* Name sets are identical between old and new.
* Uniqueness is preserved both in data and schema.

== Relational preservation evidence via bridge table (`component_features`)

=== Bridge FK integrity
|===
|Check|Result

|Old bridge FK orphans (`digipal_component_features.component_id`)
|0

|New bridge FK orphans (`symbols_structure_component_features.component_id`)
|0
|===

Conclusion:

* Component references are referentially valid in both old and new bridge tables.

=== Per-component usage count preservation (through bridge)
Compared counts of rows per `component_id` in:

* old `digipal_component_features`
* new `symbols_structure_component_features`

|===
|Check|Result

|Component IDs compared
|12

|Usage count same
|12

|Usage count differs
|0
|===

Optional mismatch sample was empty.

Conclusion:

* For every component used in the bridge, usage/link counts were preserved exactly.

=== Used vs unused component profile (through bridge)
|===
|Metric|Old|New

|Total components
|15
|15

|Used components (present in bridge)
|12
|12

|Unused components (not present in bridge)
|3
|3
|===

Interpretation:

* The migration preserved not only component rows, but also the usage footprint of components in downstream relations.

== Conclusion
This is a clean 1:1 reference-table migration with preserved semantics and preserved downstream usage:

* `id` and `name` are preserved exactly
* `id` is widened (`int4` → `int8`)
* `name` length is narrowed (`varchar(128)` → `varchar(100)`) with no truncation risk
* audit fields (`created`, `modified`) are intentionally dropped
* uniqueness semantics on `name` are preserved
* bridge-table evidence confirms component relationships/usages were preserved exactly (12 used, 3 unused)
