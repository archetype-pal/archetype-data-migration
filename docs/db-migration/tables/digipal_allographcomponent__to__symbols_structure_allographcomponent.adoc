= public.digipal_allographcomponent (old) → public.symbols_structure_allographcomponent (new)

== Summary
* Rows: 81 → 80 (one-row reduction due to deduplication)
* IDs mostly preserved: 80 retained rows join 1:1 by `id` with exact shared FK values
* 1 old ID missing in new (`id=46`); no extra new IDs
* Pair-set semantics preserved exactly at relationship level: distinct `(allograph_id, component_id)` pairs are identical (0 missing / 0 extra)
* Old table contains 1 duplicate pair (`allograph_id=15`, `component_id=8`, rows=2); new table contains no duplicate pairs
* New schema introduces `UNIQUE (allograph_id, component_id)`, strongly indicating intentional deduplication during migration
* Legacy audit columns (`created`, `modified`) removed
* No FK orphan issues on old or new side

== Counts
|===
|DB|Table|Rows
|old|public.digipal_allographcomponent|81
|new|public.symbols_structure_allographcomponent|80
|===

== Direct row identity preservation
|===
|Check|Result
|Old IDs missing in new|1
|Missing old ID sample|46
|New IDs not in old|0
|Joined rows by `id` (old↔new)|80
|Joined rows where `allograph_id` unchanged|80
|Joined rows where `component_id` unchanged|80
|Conclusion|80 rows preserved 1:1 by `id`; one old row removed in migration
|===

Interpretation:

* This is not a full 1:1 row-count migration.
* For the 80 retained IDs, shared columns are preserved exactly.
* The single missing row is explained by duplicate relationship deduplication (see below).

== Column model comparison

=== Common columns
|===
|Column|Old|New|Notes
|id|int4 NOT NULL|int8 NOT NULL|type widened
|allograph_id|int4 NOT NULL|int8 NOT NULL|type widened; FK target renamed/migrated
|component_id|int4 NOT NULL|int8 NOT NULL|type widened; FK target renamed/migrated
|===

=== Old-only columns (removed in new model)
* `created`
* `modified`

=== New-only columns
* None

== Constraints and indexes

=== Constraints
Old:

* Primary key on `id`
* FK `allograph_id` → `digipal_allograph(id)`
* FK `component_id` → `digipal_component(id)`

New:

* Primary key on `id`
* FK `allograph_id` → `symbols_structure_allograph(id)`
* FK `component_id` → `symbols_structure_component(id)`
* Unique constraint on `(allograph_id, component_id)`

Observation:

* The new model adds an explicit uniqueness rule at pair level (`allograph_id`, `component_id`) that did not exist in the old schema.
* This aligns with the observed one-row reduction and duplicate cleanup.

=== Indexes
Old:

* PK index
* index on `allograph_id`
* index on `component_id`

New:

* PK index
* index on `allograph_id`
* index on `component_id`
* unique index on `(allograph_id, component_id)`

== Data quality / blocker checks (generic same-column checks)
|===
|Check|Result
|Old NULLs in common new NOT NULL column `id`|0
|Old NULLs in common new NOT NULL column `allograph_id`|0
|Old NULLs in common new NOT NULL column `component_id`|0
|Varchar shrink blockers (same-name columns)|None (no comparable varchar columns)
|===

Interpretation:

* No blockers for direct transfer of shared columns.
* The migration difference is semantic/deduplication, not nullability or type-fit failure.

== Relationship-level semantic migration verification

=== Duplicate pair profiling
Old duplicate raw pairs (`allograph_id`, `component_id`):

* `(15, 8)` appears **2** times

New duplicate raw pairs (`allograph_id`, `component_id`):

* none

Summary:
|===
|Side|Duplicate pair rows
|old|1
|new|0
|===

Conclusion:

* The old table contained one duplicate relationship row.
* The new table is normalized to unique pairs.

=== Pair-set preservation (ignoring row IDs)
Distinct pair comparison:
|===
|Check|Result
|Old distinct pairs missing in new|0
|New distinct pairs not in old|0
|===

Conclusion:

* The migration preserves the complete set of logical allograph↔component relationships.
* The row-count drop is due to deduplication only, not loss of relationship coverage.

=== Usage-count comparison by dimension
|===
|Dimension|IDs compared|Usage count same|Usage count diff
|allograph_id|54|53|1
|component_id|13|12|1
|===

Mismatch samples:

* `allograph_id=15`: old `2`, new `1`
* `component_id=8`: old `12`, new `11`

Interpretation:

* Both dimension mismatches point to the same deduplicated pair event.
* This is exactly what would be expected from collapsing one duplicate `(allograph_id=15, component_id=8)` row.

== Referential integrity checks
|===
|Check|Result
|Old allograph FK orphans|0
|Old component FK orphans|0
|New allograph FK orphans|0
|New component FK orphans|0
|===

Conclusion:

* Both old and new bridge tables are referentially clean.

== Migration behavior interpretation
Strong evidence supports the following migration behavior:

* preserve existing row IDs where possible
* remove duplicate relationship rows that violate logical uniqueness
* enforce uniqueness going forward via `UNIQUE (allograph_id, component_id)`

The single missing old row (`id=46`) is therefore attributable to duplicate-pair deduplication (consistent with:
`81 → 80` row counts, exactly one old duplicate pair, identical distinct pair-set, and one-count deltas on the affected allograph/component usage totals).

== Conclusion
This table is a **near-1:1 bridge migration with intentional normalization**:

* shared identifiers/FKs are preserved exactly for all retained rows (80/80 joined by `id`)
* one duplicate old relationship row is removed
* logical relationship coverage is fully preserved (distinct pair-set unchanged)
* new schema improves integrity by adding `UNIQUE (allograph_id, component_id)`
* audit timestamps are dropped as part of schema simplification
