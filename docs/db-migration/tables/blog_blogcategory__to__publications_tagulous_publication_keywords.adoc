= public.blog_blogcategory (old) → public.publications_tagulous_publication_keywords (new)

== Summary
* Rows: 3 → 3 (1:1)
* IDs preserved: all 3 rows match by `id`
* Semantic mapping preserved: `title → name`, `slug → slug`
* New Tagulous fields are populated consistently:
** `count` matches actual usage in `publications_publication_keywords`
** `protected` is present (all `false`)
* Old `site_id` scoping is removed (new table has no site FK), consistent with a single-site/publications-focused model
* Slug length shrinks (old up to 2000 → new 50) with **no truncation blockers** in this dataset

== Counts
|===
|DB|Table|Rows
|old|public.blog_blogcategory|3
|new|public.publications_tagulous_publication_keywords|3
|===

== Direct row identity preservation
|===
|Check|Result
|Old IDs missing in new|0
|New IDs not in old|0
|Joined rows by `id`|3
|Conclusion|Perfect 1:1 migration with preserved IDs
|===

== Old→New mapping (all rows)
|===
|id|old.title|old.slug|new.name|new.slug|new.count|new.protected
|1|Blog|blog|Blog|blog|19|false
|2|News|news|News|news|38|false
|3|Feature Article|feature-of-the-month|Feature Article|feature-of-the-month|10|false
|===

== Column model comparison

=== Common columns
|===
|Column|Old|New|Notes
|id|int4 NOT NULL|int8 NOT NULL|type widened in new
|slug|varchar(2000) NULL|varchar(50) NOT NULL|slug length reduced; NULL disallowed in new
|title/name|title varchar(500) NOT NULL|name varchar(255) NOT NULL|renamed + length adjusted
|===

=== Old-only columns (removed in new model)
* `site_id` (FK to `django_site`)

=== New-only columns
* `count` (int4 NOT NULL) — cached usage count
* `protected` (bool NOT NULL)

== Constraints and indexes

=== Constraints
Old:

* Primary key on `id`
* FK `site_id` → `django_site(id)` (site scoping)

New:

* Primary key on `id`
* Unique constraint on `name`
* Unique constraint on `slug`

Observation:

* The new table enforces uniqueness on both the human label (`name`) and the stable identifier (`slug`).
* Site scoping is removed, indicating categories/tags are now global to the application context.

== Data quality / blocker checks
|===
|Check|Result
|Old slugs NULL|0
|Old slugs exceeding new max length (50)|0
|Conclusion|No blockers for slug shrink in this dataset
|===

== Tag usage count correctness
|===
|id|slug|stored_count|actual_count|matches
|1|blog|19|19|true
|2|news|38|38|true
|3|feature-of-the-month|10|10|true
|===

Interpretation:

* `count` is consistent with the number of rows referencing each tag in `publications_publication_keywords`.
* This suggests either:
** the backfill explicitly set `count`, or
** it was updated after insertion.

== Conclusion
This is a **clean 1:1 migration** of blog categories into Tagulous keywords:

* IDs preserved
* slugs preserved and fit within the new constraint
* counts are accurate and consistent with usage
* site-specific legacy scoping has been removed in the new model
