= public.auth_user_groups (old) → public.auth_user_groups (new)

== Summary
* Rows: 17 → 0 (complete loss of bridge rows in new table)
* No old row IDs were preserved: all 17 old rows are missing in new
* No extra new rows exist
* Distinct `(user_id, group_id)` membership pairs are not preserved: 17 old pairs missing, 0 extra new pairs
* Old and new schemas are structurally equivalent for the bridge purpose, with only `id` widened from `int4` to `int8`
* No duplicate membership pairs exist on either side
* No FK orphan issues exist on either side
* Therefore, this is not a migrated bridge table; it is an empty/reset table in the new database

== Counts
|===
|DB|Table|Rows
|old|public.auth_user_groups|17
|new|public.auth_user_groups|0
|===

== Direct row identity preservation
|===
|Check|Result
|Old IDs missing in new|17
|New IDs not in old|0
|Joined rows by `id` (old↔new)|0
|Joined rows where `user_id` unchanged|0
|Joined rows where `group_id` unchanged|0
|Conclusion|No old bridge rows were retained by `id`
|===

Interpretation:

* Because the new table is empty, there is no row-level preservation at all.
* This is a full loss of old membership-link rows, not a partial remapping.

== Column model comparison

=== Common columns
|===
|Column|Old|New|Notes
|id|int4 NOT NULL|int8 NOT NULL|type widened
|user_id|int4 NOT NULL|int4 NOT NULL|same semantics; FK target unchanged in name
|group_id|int4 NOT NULL|int4 NOT NULL|same semantics; FK target unchanged in name
|===

=== Old-only columns
* None

=== New-only columns
* None

== Constraints and indexes

=== Constraints
Old:

* Primary key on `id`
* FK `user_id` → `auth_user(id)`
* FK `group_id` → `auth_group(id)`
* Unique constraint on `(user_id, group_id)`

New:

* Primary key on `id`
* FK `user_id` → `auth_user(id)`
* FK `group_id` → `auth_group(id)`
* Unique constraint on `(user_id, group_id)`

Observation:

* The bridge-table design is materially the same in old and new.
* The absence of rows is therefore a data-migration outcome, not a schema incompatibility issue.

=== Indexes
Old:

* PK index
* index on `user_id`
* index on `group_id`
* unique index on `(user_id, group_id)`

New:

* PK index
* index on `user_id`
* index on `group_id`
* unique index on `(user_id, group_id)`

== Data quality / blocker checks
|===
|Check|Result
|Old NULLs in common new NOT NULL column `id`|0
|Old NULLs in common new NOT NULL column `user_id`|0
|Old NULLs in common new NOT NULL column `group_id`|0
|Varchar shrink blockers|None (no varchar columns)
|===

Interpretation:

* There were no nullability blockers preventing migration of the old rows.
* The absence of migrated rows is not explained by basic column-fit issues.

== Duplicate-pair profiling
Old duplicate raw pairs (`user_id`, `group_id`):

* none

New duplicate raw pairs (`user_id`, `group_id`):

* none

Summary:
|===
|Side|Duplicate pair rows
|old|0
|new|0
|===

Conclusion:

* The old bridge set was already clean.
* No deduplication explanation exists for the row drop.

== Relationship-level semantic comparison

=== Distinct pair-set comparison
|===
|Check|Result
|Old pairs missing in new|17
|New pairs not in old|0
|===

Optional missing old pairs:

* `(2,1)`, `(2,2)`
* `(4,1)`, `(4,2)`
* `(5,1)`, `(5,2)`
* `(6,1)`, `(6,2)`
* `(7,1)`, `(7,2)`
* `(8,1)`, `(8,2)`
* `(9,1)`, `(9,2)`
* `(11,1)`
* `(14,1)`, `(14,2)`

Conclusion:

* No logical user↔group memberships from the old bridge survived into the new bridge.
* There is no compensating set of new-only memberships.

=== Usage-count comparison by group
|===
|Check|Result
|Group IDs in union|2
|Group IDs on both sides|0
|Group IDs old-only|2
|Group IDs new-only|0
|Usage count same|0
|Usage count diff|2
|===

Mismatch sample:
|===
|group_id|old_rows|new_rows
|1|9|NULL
|2|8|NULL
|===

Interpretation:

* Both legacy groups lost all membership assignments in the new bridge table.

=== Usage-count comparison by user
|===
|Check|Result
|User IDs in union|9
|User IDs on both sides|0
|User IDs old-only|9
|User IDs new-only|0
|Usage count same|0
|Usage count diff|9
|===

Mismatch sample:
|===
|user_id|old_rows|new_rows
|2|2|NULL
|4|2|NULL
|5|2|NULL
|6|2|NULL
|7|2|NULL
|8|2|NULL
|9|2|NULL
|11|1|NULL
|14|2|NULL
|===

Interpretation:

* Every legacy user-group membership represented in this bridge table disappeared in the new database.

=== Coverage summary
|===
|Check|Result
|Old total rows|17
|New total rows|0
|Old used users|9
|New used users|0
|Old used groups|2
|New used groups|0
|===

== Referential integrity checks
|===
|Check|Result
|Old user FK orphans|0
|Old group FK orphans|0
|New user FK orphans|0
|New group FK orphans|0
|===

Conclusion:

* The old bridge rows were referentially valid.
* The empty new table is not caused by FK corruption.

== Interpretation
This table shows a complete break in user↔group membership migration:

* the bridge schema exists in the new database
* both referenced endpoint tables (`auth_user`, `auth_group`) also exist
* the old bridge data is clean and constraint-valid
* yet the new bridge contains no rows at all

That combination strongly indicates that old user-group memberships were not migrated into the new system.

Context from the paired `auth_group` analysis reinforces this:

* the two group master rows themselves are present in both databases
* but their membership counts drop from `9 / 8` in old to `0 / 0` in new

So the loss is specifically at the membership-link layer, not at the group-definition layer.

== Conclusion
This table is a **non-migrated bridge table**:

* schema is preserved
* data is not preserved
* all 17 old user↔group membership rows are absent in the new database
* no deduplication, remapping, or semantic replacement is evidenced inside this table
* the result is a full reset/empty-state of group memberships in the new environment
