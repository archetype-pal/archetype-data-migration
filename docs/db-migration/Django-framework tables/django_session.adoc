= public.django_session (old) → public.django_session (new)

== Summary
* Rows: 604 → 5
* Primary key is `session_key` on both sides; this table has no numeric `id`, so identity must be checked by `session_key`
* No session-key overlap at all: 604 old session keys missing in new; 5 new session keys not in old
* Joined rows by primary key: 0
* No duplicate `session_key` values on either side
* Schema is effectively unchanged at column level
* This is not a migrated data carry-over table; it behaves like an operational/runtime table that was recreated independently in the new environment
* All old sessions were expired at query time; all 5 new sessions were also expired at query time
* Strong temporal discontinuity supports regeneration rather than migration:
** old max `expire_date` = `2024-07-16 10:57:54.350684+00`
** new min `expire_date` = `2025-02-27 15:08:22.075427+00`

== Counts
|===
|DB|Table|Rows
|old|public.django_session|604
|new|public.django_session|5
|===

== Direct row identity preservation
|===
|Check|Result
|Identity key used|`session_key`
|Old session keys missing in new|604
|New session keys not in old|5
|Joined rows by `session_key`|0
|Conclusion|No session rows were migrated by primary key
|===

Interpretation:

* Because `django_session` is keyed by `session_key`, `id`-based checks are not applicable.
* There is zero overlap between old and new primary keys.
* The new table is a fresh session store, not a row-preserving migration of the old one.

== Column model comparison

=== Common columns
|===
|Column|Old|New|Notes
|session_key|varchar(40) NOT NULL|varchar(40) NOT NULL|primary key on both sides
|session_data|text NOT NULL|text NOT NULL|same type / nullability
|expire_date|timestamptz NOT NULL|timestamptz NOT NULL|same type / nullability
|===

=== Old-only columns
* None

=== New-only columns
* None

== Constraints and indexes

=== Constraints
Old:

* Primary key on `session_key`

New:

* Primary key on `session_key`

Observation:

* Structural design is unchanged.

=== Indexes
Old:

* PK index on `session_key`
* index on `expire_date`
* pattern index on `session_key`

New:

* PK index on `session_key`
* index on `expire_date`
* pattern index on `session_key`

Observation:

* Index coverage is equivalent.
* Differences are naming-pattern differences only (legacy vs newer Django-generated index names).

== Data quality / blocker checks
|===
|Check|Result
|Old NULLs in `session_key`|0
|Old NULLs in `session_data`|0
|Old NULLs in `expire_date`|0
|Old values exceeding new varchar length for `session_key` (40)|0
|===

Interpretation:

* There are no schema-fit blockers.
* The absence of migrated rows is not caused by nullability or length incompatibility.

== Duplicate-key checks
|===
|Check|Result
|Old duplicate `session_key` groups|0
|New duplicate `session_key` groups|0
|===

Conclusion:

* Both tables are internally clean as session stores.

== Semantic set comparison by primary key
|===
|Check|Result
|Old session keys missing in new|604
|New session keys not in old|5
|Shared session keys|0
|===

Conclusion:

* The old and new tables represent completely different session populations.

== Joined value comparison
|===
|Check|Result
|Total joined by `session_key`|0
|`session_data` exact match rows|0
|`expire_date` exact match rows|0
|Exact raw same rows|0
|===

Interpretation:

* No row-level content comparison is possible because there are no shared session keys.

== Session expiry profile

=== Old
|===
|Check|Result
|Total rows|604
|Expired at query time|604
|Unexpired at query time|0
|Min `expire_date`|2014-11-18 20:09:44.350911+00
|Max `expire_date`|2024-07-16 10:57:54.350684+00
|===

=== New
|===
|Check|Result
|Total rows|5
|Expired at query time|5
|Unexpired at query time|0
|Min `expire_date`|2025-02-27 15:08:22.075427+00
|Max `expire_date`|2026-02-13 11:05:33.574785+00
|===

Interpretation:

* The old environment contains only expired legacy sessions.
* The new environment contains a tiny, separate set of newer sessions, also expired at query time.
* The date ranges do not overlap, reinforcing that the new table was independently populated by application runtime.

== Migration behavior interpretation
Strong evidence supports the following interpretation:

* `django_session` was not migrated as persistent business data
* the new environment started with an empty/fresh session store
* a small number of sessions were generated locally in the new system after deployment/use
* this table should be treated as environment-specific operational state, not as a domain-data migration target

== Conclusion
This table is a **schema-preserved but data-non-migrated operational table**:

* structure is effectively unchanged
* no old session rows were carried over
* no primary-key overlap exists between environments
* the new table contains only a small, separately generated set of runtime sessions
* this is expected behavior for `django_session` and should not be treated as migration loss of business data
